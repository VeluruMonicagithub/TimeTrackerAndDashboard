// Copy this file to `firebase-config.js` and replace the placeholder values
// Then include it in `index.html` before `firebase.js`:
// <script src="firebase-config.js"></script>

// Example Firebase config — replace with your project's values
window.FIREBASE_CONFIG = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT.appspot.com",
  messagingSenderId: "SENDER_ID",
  appId: "APP_ID"
};

// Note: do NOT commit your real `firebase-config.js` to a public repo.